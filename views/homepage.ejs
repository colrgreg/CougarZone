<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CougarZone - Your Gateway to TD Arena Events</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        /* Header */
        .header {
            padding: 1.5rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1.5rem;
            font-weight: bold;
            text-decoration: none;
            color: white;
        }

        .logo-icon {
            font-size: 2rem;
        }

        .nav-buttons {
            display: flex;
            gap: 1rem;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s;
            display: inline-block;
        }

        .btn-outline {
            background: transparent;
            border: 2px solid white;
            color: white;
        }

        .btn-outline:hover {
            background: white;
            color: #764ba2;
            transform: translateY(-2px);
        }

        .btn-solid {
            background: white;
            color: #764ba2;
        }

        .btn-solid:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(255, 255, 255, 0.3);
        }

        /* Hero Section */
        .hero {
            max-width: 1200px;
            margin: 0 auto;
            padding: 4rem 2rem;
            text-align: center;
            min-height: calc(100vh - 100px);
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .hero-icon {
            font-size: 8rem;
            margin-bottom: 2rem;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }

        .hero h1 {
            font-size: 4rem;
            margin-bottom: 1rem;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .hero p {
            font-size: 1.5rem;
            margin-bottom: 3rem;
            opacity: 0.95;
        }

        .hero-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-large {
            padding: 1.25rem 3rem;
            font-size: 1.25rem;
        }

        /* Features Section */
        .features {
            max-width: 1200px;
            margin: 4rem auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 20px;
            text-align: center;
            transition: all 0.3s;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .feature-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.2);
        }

        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .feature-card p {
            opacity: 0.9;
            line-height: 1.6;
        }

        /* Events Preview */
        .events-preview {
            max-width: 1200px;
            margin: 4rem auto;
            padding: 0 2rem;
            text-align: center;
        }

        .events-preview h2 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .events-preview p {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        .event-types {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 2rem;
        }

        .event-type {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            padding: 1rem 2rem;
            border-radius: 50px;
            font-size: 1.5rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 2rem;
            background: rgba(0, 0, 0, 0.2);
            margin-top: 4rem;
        }

        .footer p {
            opacity: 0.8;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header {
                padding: 1rem;
                flex-direction: column;
                gap: 1rem;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .hero p {
                font-size: 1.25rem;
            }

            .hero-icon {
                font-size: 5rem;
            }

            .hero-buttons {
                flex-direction: column;
            }

            .btn-large {
                padding: 1rem 2rem;
                font-size: 1.1rem;
            }

            .features {
                grid-template-columns: 1fr;
            }

            .events-preview h2 {
                font-size: 2rem;
            }

            .event-type {
                font-size: 1.25rem;
                padding: 0.75rem 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <a href="/" class="logo">
            <span class="logo-icon">🎟️</span>
            <span>CougarZone</span>
        </a>
        <nav class="nav-buttons">
            <a href="/login" class="btn btn-outline">Log In</a>
            <a href="/signup" class="btn btn-solid">Sign Up</a>
        </nav>
    </header>

    <!-- Hero Section -->
    <main class="hero">
        <div class="hero-icon">🎟️</div>
        <h1>Welcome to CougarZone</h1>
        <p>Your ultimate portal for exclusive events at the College of Charleston</p>
        <div class="hero-buttons">
            <a href="/signup" class="btn btn-solid btn-large">Get Started</a>
            <a href="/login" class="btn btn-outline btn-large">Sign In</a>
        </div>
    </main>

    <!-- Features Section -->
    <section class="features">
        <div class="feature-card">
            <div class="feature-icon">🎯</div>
            <h3>Easy Booking</h3>
            <p>Book tickets to your favorite events with just a few clicks</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">📱</div>
            <h3>Digital Tickets</h3>
            <p>Access your tickets anytime, anywhere on your mobile device</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🔒</div>
            <h3>Secure Payment</h3>
            <p>Safe and encrypted transactions for peace of mind</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">⚡</div>
            <h3>Instant Access</h3>
            <p>Get your tickets immediately after purchase</p>
        </div>
    </section>

    <!-- Events Preview -->
    <section class="events-preview">
        <h2>Experience Amazing Events</h2>
        <p>From sports to concerts, we've got it all</p>
        <div class="event-types">
            <span class="event-type">🏀 Basketball</span>
            <span class="event-type">🎸 Concerts</span>
            <span class="event-type">🎭 Shows</span>
            <span class="event-type">📍 Local Events</span>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <p>&copy; 2025 CougarZone. All rights reserved.</p>
    </footer>
</body>
</html>
